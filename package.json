{
  "name": "benchmark-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "normalize": "node scripts/normalize.mjs",
    "dev": "npm run normalize && vite",
    "build": "npm run normalize && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "typescript": "^5.6.3",
    "vite": "^8.3.0"
  }
}
